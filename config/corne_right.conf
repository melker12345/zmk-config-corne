# Right (Peripheral) side specific config

# Peripheral automatically reports its battery to central (no config needed)
# The peripheral display will show its local battery level
# The central fetches this and can display both

